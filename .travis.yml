language: r
cache: packages
os:
- osx
addons:
  apt:
    packages:
    - libmagick++-dev
after_success:
- Rscript -e 'devtools::install(); library(texPreview); tex_opts$set(returnType = "tex"); texPreview::texPreview(obj = xtable::xtable(head(iris,10)));'
