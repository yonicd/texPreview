language: r
cache: packages

before_install:
  - tlmgr install standalone

addons:
  apt:
    packages:
      - libmagick++-dev

after_success:
- Rscript -e 'devtools::install(); library(texPreview);tex_opts$set(returnType="tex");texPreview(obj = xtable::xtable(head(iris,10)));'
